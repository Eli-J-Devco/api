<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Dashboard">
	<resultMap id="DashboardMap"
		type="com.nwm.api.entities.AlertEntity">
		<result property="id" column="id" />
		<result property="id_customer" column="id_customer" />
		<result property="id_device" column="id_device" />
		<result property="id_error" column="id_error" />
		<result property="start_date" column="start_date" />
		<result property="end_date" column="end_date" />
		<result property="asset" column="asset" />
		<result property="capacity" column="capacity" />
		<result property="status" column="status" />
		<result property="is_delete" column="is_delete" />
		<result property="created_date" column="created_date" />
		<result property="created_by" column="created_by" />
		<result property="updated_date" column="updated_date" />
		<result property="updated_by" column="updated_by" />
		<result property="id_site" column="id_site" />
		<result property="totalRecord" column="totalRecord" />
		<result property="current_time" column="current_time" />
		<result property="user_history" column="user_history" />
		
	</resultMap>
	
	<select id="getList" resultType="Map" >
		SELECT
			l.id,
			l.asset,
			el.name AS priority_name,
			l.capacity,
			l.`status`,
			l.alert_acknowledged,
			l.disable_notification,
			l.resolved,
			IF(l.`status` = 1, 'Opened', 'Closed') AS status_name,
			DATE_FORMAT(CONVERT_TZ( d.last_updated,'UTC', t.value), '%c/%e/%Y %h:%i %p') AS last_updated,
			el.`name` AS `level`,
			e.message,
			e.error_code,
			d.devicename,
			d.datatablename,
			d.view_tablename,
			d.job_tablename,
			<include refid="com.nwm.common.siteName"/> AS site_name,
			s.id AS id_site,
			SHA1(s.id) AS hash_id,
			d.id as id_device,
			h.is_hiding,
			i.icon AS icon_alert,
			el.id AS id_error_level,
			DATE_FORMAT( CONVERT_TZ( l.start_date, t.`offset`, #{offset_timezone} ), #{format_sql_long} ) AS start,
			DATE_FORMAT( CONVERT_TZ( l.start_date,'+00:00', t.`offset`) , '%m/%d/%Y %H:%i %p') AS start_date,
			DATE_FORMAT(CONVERT_TZ( l.end_date,'+00:00', t.`offset`), '%m/%d/%Y %H:%i %p') AS end_date,
			
			CASE
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL -1 DAY ) THEN TIMESTAMPDIFF( DAY, CONVERT_TZ( l.start_date, '+00:00', t.`offset` ), DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i') )
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL -1 HOUR ) THEN TIMESTAMPDIFF( HOUR, CONVERT_TZ( l.start_date, '+00:00', t.`offset` ), DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i') ) 
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL -1 MINUTE ) THEN TIMESTAMPDIFF( MINUTE, CONVERT_TZ( l.start_date, '+00:00', t.`offset` ), DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i') ) 
				ELSE 0
			END AS times_ago,

	
			CASE
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL - 1 DAY ) THEN 'day'
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL - 1 HOUR ) THEN 'hour'
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL - 1 MINUTE ) THEN 'minute' ELSE 'now'
			END AS times_ago_unit,
			
			
			<include refid="com.nwm.common.keyIndicatorField"/>
			
			
			
		FROM
			`alert` l
			LEFT JOIN error e ON e.id = l.id_error
			LEFT JOIN error_level el ON el.id = e.id_error_level
			LEFT JOIN icon i ON i.id = el.id_icon
			LEFT JOIN device d ON d.id = l.id_device
			LEFT JOIN device_group dg ON dg.id = d.id_device_group
			LEFT JOIN site s ON s.id = d.id_site
			LEFT JOIN time_zone t ON t.id = s.id_time_zone
			LEFT JOIN (
				SELECT
					id_site,
					MAX(is_hiding) AS is_hiding
				FROM site_employee_map
				GROUP BY id_site
			) h ON h.id_site = s.id
		WHERE
			e.`status` = 1
			AND e.is_delete = 0
			AND l.is_delete = 0
			AND l.status = 1
			AND (CONVERT_TZ(NOW(), '+00:00', t.offset) <![CDATA[ < ]]> s.expiration OR s.expiration IS NULL)
			
			<if test="id_sites != null and !id_sites.isEmpty()">
				AND s.id IN  (
					<foreach item="item" index="index" collection="id_sites" separator=" , ">
						#{item.id}
					</foreach>
				)
			</if>
			
			<include refid="com.nwm.common.deviceByDomainCondition"/>
	        
	        <if test="!isUserNW.booleanValue()">
				AND el.id != 14
			</if>
		GROUP BY l.id 
			
		ORDER BY
			<choose>  
	            <when test="sort_column == 'id'">
	                l.id ${order_by}
	            </when> 
	                
	            
	            <when test="sort_column == 'error_code'">
	                e.error_code ${order_by}
	            </when>
	            
	            <when test="sort_column == 'site_name'">
	                s.`name` ${order_by}
	            </when> 
	            
	            <when test="sort_column == 'devicename'">
	                d.`devicename` ${order_by}
	            </when> 
	            
	            <when test="sort_column == 'key_indicator'">
	                key_indicator ${order_by}
	            </when> 
	            
	            <when test="sort_column == 'start_date'">
	                l.start_date ${order_by}
	            </when> 
	            
	            <when test="sort_column == 'message'">
	                e.message ${order_by}
	            </when>
	            
	            
	            <otherwise>
			      l.start_date DESC
			    </otherwise>                                                  
	        </choose>
	        
	</select>
	
	<select id="getListActualvsExpected" resultType="Map" parameterType="String" >
		SELECT
			s.id AS id_site,
			<include refid="com.nwm.common.siteName"/> AS site_name,
			SHA1(s.id) AS hash_id,
			s.dc_capacity,
			ROUND(IFNULL(vir.activePower, IFNULL(meter.activePower, inv.activePower)), 1) AS power_today,
			ROUND(IFNULL(vir.expected_power_virtual, IFNULL(wea.expected_power, ep.expected)), 1) AS expected,
			ROUND(IFNULL(vir.activePower, IFNULL(meter.activePower, inv.activePower)) / IFNULL(vir.expected_power_virtual, IFNULL(wea.expected_power, ep.expected)) * 100, 1) AS a_e,
			h.is_hiding
		
		FROM
			`site` s
			LEFT JOIN time_zone t ON t.id = s.id_time_zone
			LEFT JOIN (<include refid="Portfolio.activeAndExpectedPowerByVirtualDeviceSubquery"/>) vir ON vir.id_site = s.id
			LEFT JOIN (<include refid="Portfolio.activePowerByInverterSubquery"/>) inv ON inv.id_site = s.id 
			LEFT JOIN (<include refid="Portfolio.activePowerByMeterSubquery"/>) meter ON meter.id_site = s.id
			LEFT JOIN (<include refid="Portfolio.irradianceAndExpectedPowerByWSSubquery"/>) wea ON wea.id_site = s.id
			
			LEFT JOIN (
				SELECT
					ee.id_site, 
					ee.`year`,
					CASE
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Jan' THEN ROUND((((ee.jan * 1000)/31) / (8*0.75))/1000,1)
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Feb' THEN ROUND((((ee.feb * 1000)/28) / (8*0.75))/1000,1)
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Mar' THEN ROUND((((ee.mar * 1000)/31) / (8*0.75))/1000,1)
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Apr' THEN ROUND((((ee.apr * 1000)/30) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'May' THEN ROUND((((ee.may * 1000)/31) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Jun' THEN ROUND((((ee.jun * 1000)/30) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Jul' THEN ROUND((((ee.jul * 1000)/31) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Aug' THEN ROUND((((ee.aug * 1000)/31) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Sep' THEN ROUND((((ee.sep * 1000)/30) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Oct' THEN ROUND((((ee.oct * 1000)/31) / (8*0.75))/1000,1) 
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Nov' THEN ROUND((((ee.nov * 1000)/30) / (8*0.75))/1000 ,1)
						WHEN DATE_FORMAT('2022-08-10', '%b') = 'Dec' THEN ROUND((((ee.`dec` * 1000)/31) / (8*0.75))/1000,1) 
						ELSE 0
				END AS expected

				FROM
					energy_expectations ee 
					LEFT JOIN site s ON s.id = ee.id_site
					LEFT JOIN time_zone t ON t.id = s.id_time_zone
				WHERE
					ee.`year` = DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d')	
					AND (CONVERT_TZ(NOW(), '+00:00', t.offset) <![CDATA[ < ]]> s.expiration OR s.expiration IS NULL)
			)ep ON ep.id_site = s.id
			
			LEFT JOIN (
				SELECT
					id_site,
					MAX(is_hiding) AS is_hiding
				FROM site_employee_map
				GROUP BY id_site
			) h ON h.id_site = s.id
			
			WHERE
				s.`status` = 1
				AND s.is_delete = 0
				AND (CONVERT_TZ(UTC_TIMESTAMP(), 'UTC', t.value) <![CDATA[ < ]]> s.expiration OR s.expiration IS NULL)
				<if test="id_sites != null and !id_sites.isEmpty()">
					AND s.id IN  (
						<foreach item="item" index="index" collection="id_sites" separator=" , ">
							#{item.id}
						</foreach>
					)
				</if>
				<include refid="com.nwm.common.siteByDomainCondition"/>
			
			GROUP BY s.id
			
			ORDER BY
	        <choose>  
	            <when test="sort_column == 'site_name'">
	                s.name ${order_by}
	            </when> 
	                
	            
	            <when test="sort_column == 'power_today'">
	                power_today ${order_by}
	            </when>
	            
	            <when test="sort_column == 'expected'">
	                expected ${order_by}
	            </when> 
	            
	            <when test="sort_column == 'a_e'">
	                a_e ${order_by}
	            </when> 
	            
	            <otherwise>
			      s.id DESC
			    </otherwise>                                                  
	        </choose>
	</select>
	
	
	
	<select id="getAlertSummary" resultType="com.nwm.api.entities.AlertEntity" parameterType="com.nwm.api.entities.AlertEntity">
		SELECT (
			<include refid="Alert.alertSummary" />
			<include refid="Alert.lowPriorityAlertCondition" />
		) AS lowPriority,
		(
			<include refid="Alert.alertSummary" />
			<include refid="Alert.highPriorityAlertCondition" />
		) AS highPriority,
		(
			<include refid="Alert.alertSummary" />
		) AS totalError,
		
		
		(
			SELECT
				IFNULL(ROUND(SUM(IFNULL(v.activePower, IFNULL(m.activePower, ivt.activePower))), 2), 0) AS generationNow
			FROM
				site s
				LEFT JOIN time_zone t ON t.id = s.id_time_zone
				LEFT JOIN (<include refid="Portfolio.activeAndExpectedPowerByVirtualDeviceSubquery"/>) v ON v.id_site = s.id
				LEFT JOIN (<include refid="Portfolio.activePowerByInverterSubquery"/>) ivt ON ivt.id_site = s.id 
				LEFT JOIN (<include refid="Portfolio.activePowerByMeterSubquery"/>) m ON m.id_site = s.id
			WHERE 
				s.`status` = 1
				AND s.`is_delete` = 0
				AND (CONVERT_TZ(UTC_TIMESTAMP(), 'UTC', t.value) <![CDATA[ < ]]> s.expiration OR s.expiration IS NULL)
				<if test="id_sites != null and !id_sites.isEmpty()">
					AND s.id IN  (
						<foreach item="item" index="index" collection="id_sites" separator=" , ">
							#{item.id}
						</foreach>
					)
				</if>
				<include refid="com.nwm.common.siteByDomainCondition"/>
		) AS generationNow,
		
		
		(
			SELECT
			COUNT(s.id) AS totalSite
		FROM
			site s
			WHERE s.`status` = 1
				<if test="id_sites != null and !id_sites.isEmpty()">
					AND s.id IN  (
						<foreach item="item" index="index" collection="id_sites" separator=" , ">
							#{item.id}
						</foreach>
					)
				</if>
		) AS totalSite,
		
		(
			SELECT
				SUM(s.dc_capacity) AS ratedDCCapacity
			FROM
				site s
			WHERE s.`status` = 1
				<if test="id_sites != null and !id_sites.isEmpty()">
					AND s.id IN  (
						<foreach item="item" index="index" collection="id_sites" separator=" , ">
							#{item.id}
						</foreach>
					)
				</if>
		) AS ratedDCCapacity
		
		
		
		
		
		
				
  	</select>

    <select id="getTotalEnergyTodayByVirtualDevice" resultType="com.nwm.api.entities.EnergyEntity">
        SELECT
            SUM(tdv.nvmActiveEnergy) AS actual,
            AVG(tdv.expected_power_ac) * #{duration}
        FROM
            ${table_data_virtual} tdv
        WHERE
            tdv.time BETWEEN #{start_time} AND #{end_time}
    </select>

    <select id="getTotalEnergyTodayActualByDevice" resultType="Double">
        SELECT
            SUM(actual) AS actual
        FROM
        (
            <foreach collection="list" item="item" separator="UNION ALL">
                SELECT
                    (
                    <include refid="com.nwm.common.intervalEnergy">
                        <property name="table" value="${item.datatablename}" />
                        <property name="id" value="#{item.id}" />
                    </include>
                    ) AS actual
                FROM
                    ${item.datatablename} dv
                JOIN (
                    SELECT
                        1 AS currentTimeValid,
                        0 AS leadTimeValid,
                        MIN(dv.time) AS minTime,
                        MAX(dv.time) AS maxTime,
                        NULL AS lagMaxTime
                    FROM
                        ${item.datatablename} dv
                    WHERE
                        dv.id_device = #{item.id}
                        AND dv.nvmActiveEnergy IS NOT NULL
                        AND dv.time BETWEEN #{start_time} AND #{end_time}
                    ) m ON m.minTime = dv.time AND dv.id_device = #{item.id}
            </foreach>
        ) t
    </select>

    <select id="getExpectedBySelectedPOA" resultType="Double" >
        SELECT
            SUM(expected_energy) as expected
        FROM
        (
                SELECT
                    ROUND(IF(
                        AVG(irradiance) IS NULL,
                        NULL,
                        AVG(
                        <include refid="com.nwm.common.expectedPowerByPvModel">
                            <property name="irradiance" value="irradiance" />
                            <property name="temperature" value="temperature" />
                            <property name="panelTemperature" value="panelTemperature" />
                            <property name="power" value="NULL" />
                        </include>
                        ) * #{duration}
                    ), 1) AS expected_energy
                FROM
                (
                    SELECT
                        id AS id_site,
                        AVG(temperature) AS temperature,
                        AVG(panelTemperature) AS panelTemperature,
                        AVG(irradiance) AS irradiance
                    FROM (
                        <if test="panelTemps != null and panelTemps.size() > 0">
                            <foreach collection="panelTemps" item="item" separator="UNION ALL">
                                SELECT
                                s.id,
                                AVG(dv.nvm_temperature) AS temperature,
                                AVG(dv.nvm_panel_temperature) AS panelTemperature,
                                NULL AS irradiance
                                FROM
                                ${item.datatablename} dv
                                JOIN device d ON d.id = dv.id_device
                                JOIN site s ON s.id = d.id_site
                                JOIN time_zone t ON t.id = s.id_time_zone
                                WHERE
                                s.id = #{item.id_site}
                                AND s.`status` = 1
                                AND d.`status` = 1
                                AND d.id = #{item.id}
                                AND dv.time BETWEEN #{start_time} AND #{end_time}
                            </foreach>

                        </if>

                        <if test="POAs != null and POAs.size() > 0">
                            <if test="panelTemps != null and panelTemps.size() > 0">UNION ALL</if>
                            <foreach collection="POAs" item="item" separator="UNION ALL">
                                SELECT
                                s.id,
                                NULL AS temperature,
                                NULL AS panelTemperature,
                                AVG(dv.nvm_irradiance) AS irradiance
                                FROM
                                ${item.datatablename} dv
                                JOIN device d ON d.id = dv.id_device
                                JOIN site s ON s.id = d.id_site
                                JOIN time_zone t ON t.id = s.id_time_zone
                                WHERE
                                s.id = #{item.id_site}
                                AND s.`status` = 1
                                AND d.`status` = 1
                                AND d.id = #{item.id}
                                AND dv.time BETWEEN #{start_time} AND #{end_time}
                            </foreach>
                        </if>
                    ) t
                ) t
                JOIN site s ON s.id = t.id_site
        ) t
    </select>

    <select id="getTotalEnergyTodayExpectedByDevice" resultType="Double">
        SELECT
        AVG(
        <include refid="com.nwm.common.expectedPowerByPvModel">
            <property name="irradiance" value="dv.nvm_irradiance" />
            <property name="temperature" value="dv.nvm_temperature" />
            <property name="panelTemperature" value="dv.nvm_panel_temperature" />
            <property name="power" value="NULL" />
        </include>
        ) * #{duration}
        FROM
            ${datatablename} dv
        LEFT JOIN device d ON d.id = dv.id_device
        LEFT JOIN site s ON s.id = d.id_site
        WHERE
            dv.time BETWEEN #{start_time} AND #{end_time}
    </select>
	
    <select id="getSiteMapData" resultType="com.nwm.api.entities.SiteEntity">
        SELECT
            name,
            lat,
            lng
        FROM site
        WHERE
            id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND status = 1 AND is_delete = 0
    </select>

    <select id="getTotalPowerAndCapacity" resultType="com.nwm.api.entities.SitesMetricsSummaryEntity">
        SELECT
        IFNULL(ROUND(SUM(IFNULL(v.activePower, IFNULL(m.activePower, ivt.activePower)) / 1000), 2), 0) as activePower,
        (SUM(s.ac_capacity) / 1000) AS capacity,
        (SUM(s.dc_capacity) / 1000) AS dc_capacity
        FROM
        site s
        LEFT JOIN (<include refid="Portfolio.activeAndExpectedPowerByVirtualDeviceSubquery"/>) v ON v.id_site = s.id
        LEFT JOIN (<include refid="Portfolio.activePowerByInverterSubquery"/>) ivt ON ivt.id_site = s.id
        LEFT JOIN (<include refid="Portfolio.activePowerByMeterSubquery"/>) m ON m.id_site = s.id
        <where>
            <if test="id_sites != null and !id_sites.isEmpty()">
                <foreach item="item" collection="id_sites" open="s.id IN (" close=")" separator=",">
                    #{item}
                </foreach>
            </if>
            <include refid="com.nwm.common.siteByDomainCondition"/>
        </where>
    </select>

    <select id="getInverterAvailabilityBySite" resultType="Map">
        SELECT
        s.id AS site_id,
        available.available_inverters,
        total.total_inverters,
        ROUND((available.available_inverters / total.total_inverters) * 100, 2) AS availability_percent,
        (total.total_inverters - available.available_inverters) as unavailable_inverters,
        <include refid="com.nwm.common.siteName"/> AS site_name
        FROM
        site s
        <include refid="inverterAvailability"/>
        WHERE
        s.id = #{id_site}
        <include refid="com.nwm.common.siteByDomainCondition"/>
    </select>

    <select id="getInverterAvailabilityAllSite" resultType="Map">
        SELECT
            ROUND((SUM(IFNULL(available.available_inverters, 0)) OVER() / NULLIF(SUM(IFNULL(total.total_inverters, 0)) OVER(), 0)) * 100, 2) AS total_availability_percent
        FROM
            site s
            <include refid="inverterAvailability"/>
        WHERE
            <foreach item="item" collection="id_sites" open="s.id IN (" close=")" separator=",">
                #{item}
            </foreach>
            <include refid="com.nwm.common.siteByDomainCondition"/>
        LIMIT 1
    </select>

    <sql id="inverterAvailability">
        LEFT JOIN (
        SELECT
        d.id_site,
        COUNT(*) AS total_inverters
        FROM
        device d
        WHERE
        d.`status` = 1
        AND d.is_delete = 0
        AND d.id_device_type = 1
        <include refid="com.nwm.common.deviceByDomainCondition"/>
        GROUP BY
        d.id_site
        ) AS total ON total.id_site = s.id
        LEFT JOIN (
        SELECT
        d.id_site,
        COUNT(*) AS available_inverters
        FROM
        device d
        WHERE
        d.status = 1
        AND d.is_delete = 0
        AND d.id_device_type = 1
        AND NOT EXISTS (
        SELECT
        1
        FROM
        alert a
        JOIN error e ON e.id = a.id_error
        WHERE
        a.id_device = d.id
        AND a.status = 1
        AND a.resolved = 0
        AND a.end_date is null
        AND (e.id_error_level = 32 OR e.id_error_level = 33)
        )
        GROUP BY
        d.id_site
        ) available ON available.id_site = s.id
    </sql>

    <select id="getExpectPowerByIrradianceDevice" resultType="Map">
        WITH RECURSIVE hours AS (
            SELECT CAST(#{start_time} AS DATETIME) AS category_time
            UNION ALL
            SELECT DATE_ADD(category_time, INTERVAL 1 HOUR)
            FROM hours
            WHERE DATE_ADD(category_time, INTERVAL 1 HOUR) <![CDATA[<=]]> #{end_time}
        ),

        device_power_data AS (
            SELECT
                DATE_FORMAT(dv.time, '%Y-%m-%d %H:00:00') AS category_time,
            AVG(
                <include refid="com.nwm.common.expectedPowerByPvModel">
                    <property name="irradiance" value="dv.nvm_irradiance" />
                    <property name="temperature" value="dv.nvm_temperature" />
                    <property name="panelTemperature" value="dv.nvm_panel_temperature" />
                    <property name="power" value="NULL" />
                </include>
            ) AS expect_power
            FROM ${datatablename} dv
            LEFT JOIN device d ON d.id = dv.id_device
            LEFT JOIN site s ON s.id = d.id_site
            WHERE
                dv.id_device = #{id}
                AND dv.time BETWEEN #{start_time} AND #{end_time}
            GROUP BY DATE_FORMAT(dv.time, '%Y-%m-%d %H:00:00')
        ),
        summary_data AS (
            SELECT
                category_time,
                SUM(expect_power) AS expect_power
            FROM device_power_data
            GROUP BY category_time
        )
        SELECT
            DATE_FORMAT(CONVERT_TZ(h.category_time, 'UTC',  #{time_zone_value}), '%Y-%m-%d %H:00:00') AS category_time,
            COALESCE(s.expect_power, 0) AS expect_power
        FROM hours h
        LEFT JOIN summary_data s ON s.category_time = DATE_FORMAT(h.category_time, '%Y-%m-%d %H:00:00')
        ORDER BY h.category_time
    </select>

    <select id="getActualPowerByDevice" resultType="Map">
        WITH RECURSIVE hours AS (
            SELECT CAST(#{start_time} AS DATETIME) AS category_time
            UNION ALL
            SELECT DATE_ADD(category_time, INTERVAL 1 HOUR)
            FROM hours WHERE DATE_ADD(category_time, INTERVAL 1 HOUR) <![CDATA[<=]]> #{end_time}
        ),
        device_actual_data AS (
            <foreach collection="devices" item="item" separator=" UNION ALL ">
                SELECT
                    DATE_FORMAT(dv.time, '%Y-%m-%d %H:00:00') AS category_time,
                    AVG(dv.nvmActivePower) AS actual_power
                FROM
                    ${item.datatablename} dv
                WHERE
                    dv.id_device = #{item.id}
                    AND dv.time BETWEEN #{start_time} AND #{end_time}
                GROUP BY DATE_FORMAT(dv.time, '%Y-%m-%d %H:00:00')
            </foreach>
        ),
        summary_data AS (
            SELECT
                category_time,
                SUM(actual_power) AS actual_power
            FROM device_actual_data
            GROUP BY category_time
        )
        SELECT
            DATE_FORMAT(CONVERT_TZ(h.category_time, 'UTC',  #{time_zone_value}), '%Y-%m-%d %H:00:00') AS category_time,
            COALESCE(s.actual_power, 0) AS actual_power
        FROM hours h
        LEFT JOIN summary_data s ON s.category_time = DATE_FORMAT(h.category_time, '%Y-%m-%d %H:00:00')
        ORDER BY h.category_time
    </select>

    <select id="getPowerByVirtualDevice" resultType="com.nwm.api.entities.EnergyEntity">
        WITH RECURSIVE hours AS (
            SELECT CAST(#{start_time} AS DATETIME) AS category_time
            UNION ALL
            SELECT DATE_ADD(category_time, INTERVAL 1 HOUR)
            FROM hours WHERE DATE_ADD(category_time, INTERVAL 1 HOUR) <![CDATA[<=]]> #{end_time}
        ),
        power_data AS (
            SELECT
                DATE_FORMAT(tdv.time, '%Y-%m-%d %H:00:00') AS category_time,
                AVG(tdv.nvmActivePower) AS actual,
                AVG(tdv.expected_power_ac) AS expected
            FROM ${table_data_virtual} tdv
            WHERE tdv.time BETWEEN #{start_time} AND #{end_time}
            GROUP BY DATE_FORMAT(tdv.time, '%Y-%m-%d %H:00:00')
        )
        SELECT
            DATE_FORMAT(CONVERT_TZ(h.category_time, 'UTC',  #{time_zone_value}), '%Y-%m-%d %H:00:00') AS category_time,
            COALESCE(pd.actual, 0 ) as actual_power,
            COALESCE(pd.expected, 0) as expect_power
        FROM hours h
        LEFT JOIN power_data pd ON pd.category_time = DATE_FORMAT(h.category_time, '%Y-%m-%d %H:00:00')
        ORDER BY h.category_time;
    </select>

    <select id="getExpectedPowerBySelectedPOA" resultType="Map">
        WITH RECURSIVE hours AS (
            SELECT CAST(#{start_time} AS DATETIME) AS category_time
            UNION ALL
            SELECT DATE_ADD(category_time, INTERVAL 1 HOUR)
            FROM hours
            WHERE DATE_ADD(category_time, INTERVAL 1 HOUR) <![CDATA[<=]]> #{end_time}
        ),
        device_data AS (
            SELECT
                id_site,
                category_time,
                AVG(temperature) AS temperature,
                AVG(panelTemperature) AS panelTemperature,
                AVG(irradiance) AS irradiance
            FROM (
                <if test="panelTemps != null and panelTemps.size() > 0">
                    <foreach collection="panelTemps" item="item" separator="UNION ALL">
                        SELECT
                            s.id AS id_site,
                            DATE_FORMAT(dv.time, '%Y-%m-%d %H:00:00') AS category_time,
                            AVG(dv.nvm_temperature) AS temperature,
                            AVG(dv.nvm_panel_temperature) AS panelTemperature,
                            NULL AS irradiance
                        FROM ${item.datatablename} dv
                        JOIN device d ON d.id = dv.id_device
                        JOIN site s ON s.id = d.id_site
                        WHERE s.id = #{item.id_site}
                            AND s.status = 1
                            AND d.status = 1
                            AND d.id = #{item.id}
                            AND dv.time BETWEEN #{start_time} AND #{end_time}
                        GROUP BY category_time, s.id
                    </foreach>
                    UNION ALL
                </if>
                <if test="POAs != null and POAs.size() > 0">
                    <foreach collection="POAs" item="item" separator="UNION ALL">
                        SELECT
                            s.id AS id_site,
                            DATE_FORMAT(dv.time, '%Y-%m-%d %H:00:00') AS category_time,
                            NULL AS temperature,
                            NULL AS panelTemperature,
                            AVG(dv.nvm_irradiance) AS irradiance
                        FROM ${item.datatablename} dv
                        JOIN device d ON d.id = dv.id_device
                        JOIN site s ON s.id = d.id_site
                        WHERE s.id = #{item.id_site}
                            AND s.status = 1
                            AND d.status = 1
                            AND d.id = #{item.id}
                            AND dv.time BETWEEN #{start_time} AND #{end_time}
                        GROUP BY category_time, s.id
                    </foreach>
                </if>
            ) x
            GROUP BY id_site, category_time
        ),
        expected_power_data AS (
            SELECT
                dd.category_time,
            ROUND(
                AVG(
                    <include refid="com.nwm.common.expectedPowerByPvModel">
                        <property name="irradiance" value="irradiance"/>
                        <property name="temperature" value="temperature"/>
                        <property name="panelTemperature" value="panelTemperature"/>
                        <property name="power" value="NULL"/>
                    </include>
                ), 1
            ) AS expected_power
            FROM device_data dd
            LEFT JOIN site s ON s.id = dd.id_site
            GROUP BY dd.category_time
        )

        SELECT
            DATE_FORMAT(CONVERT_TZ(h.category_time, 'UTC',  #{time_zone_value}), '%Y-%m-%d %H:00:00') AS category_time,
            COALESCE(ep.expected_power, 0) AS expected_power
        FROM hours h
        LEFT JOIN expected_power_data ep ON ep.category_time = DATE_FORMAT(h.category_time, '%Y-%m-%d %H:00:00')
        ORDER BY h.category_time
    </select>
</mapper>