<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mobile.common.time">
    <sql id="convertTimeByDataSendInterval">
        <choose>
            <when test="request.dataSendInterval == 2">
                FROM_UNIXTIME(( UNIX_TIMESTAMP( ${time} ) DIV 900  )* 900, '%Y-%m-%d %H:%i' )
            </when> 
            <otherwise>
                "all"
            </otherwise>
        </choose>
    </sql>
</mapper>